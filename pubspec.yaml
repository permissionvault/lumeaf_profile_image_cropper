name: lumeaf_profile_image_cropper
description: "Profile Image Cropper"
version: 1.0.0
homepage:

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.5+1
  image: ^4.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
